<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="constellation">
	<select id="selectCstYearly" resultType="ConstellationDTO">
		SELECT	*
		FROM CONSTELLATION_TB
		WHERE cst_type = "yearly"
			 AND constellation = #{constellation}
	</select>
	
	<select id="selectCstMonthly" resultType="ConstellationDTO">
		SELECT	*
		FROM CONSTELLATION_TB
		WHERE cst_type = "monthly"
			 AND cst_year_month = MONTH(NOW())
			 AND constellation = #{constellation}
	</select>

</mapper>